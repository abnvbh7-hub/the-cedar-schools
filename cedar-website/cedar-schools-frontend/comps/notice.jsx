export default function NoticeScroll({annouce}){
  return (
    <div className="notice-con">
      <div className="notice-track">
        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>

        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>
        <div className="notice">{annouce}</div>
      </div>
    </div>
  )
}